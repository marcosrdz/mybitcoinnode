import React, { Component } from 'react'
import BitcoinSettingsPanel from './Panels/BitcoinSettingsPanel'

export default class BitcoinSettings extends Component {
  render() {
    return (
      <div>
        <BitcoinSettingsPanel />
      </div>
    )
  }
}